describe('grapp_ref.ts', () => {
  describe('GrappRef', () => {
    describe('new GrappRef()' , () => {
      it('should throw a TypeError when first argument has no injector', () => {
        expect(true).toBeTruthy();
      })
    });
  });
});
